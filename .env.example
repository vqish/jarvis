# ==============================================================================
# JARVIS AI STUDY ASSISTANT — FIREBASE CONFIGURATION TEMPLATE
# ==============================================================================
# 
# Instructions:
# 1. Create/Open your Firebase project at https://console.firebase.google.com/
# 2. Go to: Project Settings (gear icon) → General → Your apps
# 3. Under "Firebase SDK snippet", select the "Config" radio button
# 4. Copy the matching values into your local `.env.local` file.
#
# IMPORTANT:
# - Do NOT commit `.env.local` to Git.
# - Only client-side credentials belong in VITE_* variables.
# - Do NOT put Firebase Admin SDK private keys or server secrets here.
# ==============================================================================

# Firebase Web App Configuration
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
